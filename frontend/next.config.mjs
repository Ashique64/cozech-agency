/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    reactCompiler: true,
    images: {
        domains: ["www.cozech.com", "cozech.com"],
    },
};

export default nextConfig;
